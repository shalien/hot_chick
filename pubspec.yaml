name: hot_chick
description: A starting point for Dart libraries or applications.
version: 0.1.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  encrypt: ^4.0.2
  crypto: ^2.1.5
  file_directory_ext: ^0.1.3

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
